"""Optional third-party framework integrations, each behind its own extra:

    pip install "foveance[litellm]"    -> foveance.integrations.litellm
    pip install "foveance[langchain]"  -> foveance.integrations.langchain

Nothing in this package is imported by ``foveance`` itself; the core stays dependency-free.
"""
